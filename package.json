{
  "name": "react-ssr",
  "version": "1.0.0",
  "description": "`mkdir react`",
  "main": "index.js",
  "scripts": {
    "build": "esbuild server/src/server.tsx --bundle --outfile=built/server.js --platform=node"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "esbuild": "^0.14.0",
    "express": "^4.17.1",
    "react": "^17.0.2",
    "react-dom": "^17.0.2"
  }
}
